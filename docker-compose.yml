services:
  server:
    build: ./server
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - ./server:/app

  # Phase B: uncomment when migrating off the demo storage backend (SPEC §6.1)
  # postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: neon_backgammon
  #     POSTGRES_USER: neon
  #     POSTGRES_PASSWORD: neon
  #   ports:
  #     - "5432:5432"
  #
  # redis:
  #   image: redis:7
  #   ports:
  #     - "6379:6379"
